## 1. 设置页分类与信息架构调整

- [x] 1.1 扩展 `SettingsCategory`，新增 `suppliers` 分类，并为设置页左侧菜单接入“供应商”入口
- [x] 1.2 将现有 `providers` 页面收敛为“API 配置”页面，只保留内置供应商 API Key、GRSAI 相关配置与说明文档
- [x] 1.3 在 `SettingsDialog` 中移除旧的内嵌式 `CustomProviderSection`，改为挂载独立的“供应商”页面容器
- [x] 1.4 新增或更新设置页测试，验证“API 配置”“供应商”两个入口可见且切换到正确内容区

## 2. 供应商列表页与新增 / 编辑弹窗

- [x] 2.1 新建供应商列表页组件，完成顶部“添加供应商”按钮、空状态、供应商名称、可用模型摘要和列表行操作按钮展示
- [x] 2.2 新建可复用的供应商编辑弹窗组件，支持新增与编辑两种模式，并复用现有字段与 `validateCustomProviders` 校验逻辑
- [x] 2.3 将供应商新增 / 编辑改为弹窗内即时保存：点击“保存”后直接写入 `customProviders`、关闭弹窗并刷新列表，不再依赖设置页底部二次保存
- [x] 2.4 新增或更新前端测试，覆盖供应商列表渲染、空状态、新增弹窗、编辑弹窗和保存后的列表同步

## 3. 删除确认与生图入口联动

- [x] 3.1 新建删除确认弹窗，并将列表行“删除”按钮接入二次确认流程；只有确认后才真正从 `customProviders` 中删除
- [x] 3.2 调整生图流程中的“去配置供应商”跳转逻辑：自定义供应商跳到 `suppliers`，内置供应商继续跳到 `providers`
- [x] 3.3 保持 `customProviders`、`runtimeRegistry`、`ModelParamsControls` 的运行时契约不变，验证编辑、删除或禁用模型后的 fallback 行为，确保节点不会因设置页重构而失效
- [x] 3.4 新增或更新联动测试，覆盖删除确认、自定义供应商缺配置跳转 `suppliers`，以及生图侧继续参与供应商 / 模型二级选择

## 4. 文案与回归验证

- [x] 4.1 更新中英文 i18n 文案，覆盖“供应商”页面、弹窗标题、按钮、删除确认、空状态和旧 `providers` 分类重命名
- [x] 4.2 新增或更新语言 / UI 回归测试，确保新增 key 在 `zh/en` 中同时存在且供应商管理流程文案可用
- [x] 4.3 完成定向 Vitest、`npx tsc --noEmit` 与前端构建验证，确认设置页拆分和生图联动无类型或打包回归
