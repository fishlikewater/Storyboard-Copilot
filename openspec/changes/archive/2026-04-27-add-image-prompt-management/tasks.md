## 1. Prompt Template Data Model

- [x] 1.1 新增提示词模板配置类型、归一化与校验辅助模块，并为标题/正文/唯一性规则补充单元测试
- [x] 1.2 扩展 `settingsStore` 持久化字段、migration 和 action，使提示词模板可以稳定读写

## 2. Prompt Management UI

- [x] 2.1 在 `TitleBar` 与 `App.tsx` 中新增顶栏提示词管理入口和独立弹窗开关状态
- [x] 2.2 新建提示词管理弹窗，完成空状态、标题/正文摘要展示、添加按钮、编辑按钮、删除按钮和保存/删除后的列表刷新
- [x] 2.3 新建提示词新增/编辑弹窗与删除确认弹窗，并接入字段校验与二次确认流程
- [x] 2.4 更新提示词管理相关 i18n 文案与弹窗交互测试

## 3. AI Image Shortcut Integration

- [x] 3.1 在 `ImageEditNode` 中接入提示词快捷标题区，实现最多两行展示与内部滚动
- [x] 3.2 实现点击快捷标题后将模板内容应用到当前节点 prompt 的逻辑：空文本直接写入，非空文本先插入空行再追加，并确保文本框草稿和节点数据保持同步
- [x] 3.3 新增或更新 `ImageEditNode` 交互测试，覆盖无模板隐藏、模板展示和点击追加行为

## 4. Validation

- [x] 4.1 运行提示词配置、提示词管理弹窗与 `ImageEditNode` 的定向 Vitest 回归
- [x] 4.2 运行 `npx tsc --noEmit`，确认新增状态和 UI 变更无类型回归
- [x] 4.3 如改动影响构建入口，补跑前端构建验证
